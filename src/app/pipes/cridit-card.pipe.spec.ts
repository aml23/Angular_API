import { CriditCardPipe } from './cridit-card.pipe';

describe('CriditCardPipe', () => {
  it('create an instance', () => {
    const pipe = new CriditCardPipe();
    expect(pipe).toBeTruthy();
  });
});
